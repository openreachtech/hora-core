<!-- English: [adopting.md](./adopting.md) — 片方を直したら、同じコミットでもう片方も直してください -->

# 既存プロジェクトへの Hora Kit 適用

Hora Kit は通常「そこから始めるテンプレート」として出会います。こちらはもう一方の場合です — renchan のバックエンドと furo のフロントエンドが既にあり、動くコードを持っていて、その周りにキットを被せたい。

**既存リポジトリは何ひとつ奪われません。** 履歴もブランチも設定もコードもそのままです。Hora Kit はそれらの**外側**に立ち、仕様書と計画と記録を持つリポジトリです。

---

## 適用して何が手に入るのか

「残りをキットが作ってくれる」ではありません。それは後の話です。**最初に手に入るのは、この製品が今何をできるのかという事実です。**

「既に作られている」と宣言された機能は、作り方を述べた17の関所を飛ばしますが、**検収には必ず入ります。** つまり適用後の最初の掃引は、既存製品全体を、その製品自身が掲げるユースケースに対して受入レビューすることになります — どこに到達でき、何が揃っていて、失敗したときに本当のことを言うか。**これは `Baseline: verified` のもとでの話で**、機能を未検収のまま棚卸しだけしておく選択もあります（下）。

**指摘が出ることを見込んでください。そしてそれが、適用した価値そのものです。**

その後は、新しい機能が18関所を1つずつ通っていきます。

---

## 最初に、2つの適用のどちらかを決める

**以下のどの手順より先に、1つの問いに答えてください：仕様書とコードが食い違ったとき、どちらが正しいのか。** この文書の残り全部がその答えで分岐し、答えは仕様書の1行 — `Existing assets` 節の `Authority:` — に書かれます（[`spec-format.md`](../.claude/skills/hora/references/spec-format.md) の "Existing assets"）。

| | **`as-built`** — 実装が正 | **`to-spec`** — 仕様が正 |
|---|---|---|
| あなたの状況 | 製品は動いていて、その動きがあなたの望むもの。**現状を版として固定し**、そこから育てるために適用する | 誰かが書いた仕様に向けて**実装の途中**。残りの距離を詰めるために適用する |
| 仕様書が記述するもの | 今日動いている製品 | あるべき製品 |
| 両者の食い違い | 仕様書の文面を訂正する | タスク — コードを直す |
| 尋ねられる質問 | **最小限**：宣言1問、導出された `built:` 表の機能ごとの確認（選択するだけ、作文しない）、読み取りのバッチ確認、そして未認証で到達できる操作1件につき1問 | 通常の7ステージの対話 |
| ユースケースと受入基準 | **動いているシステムから草案され**、作文ではなく訂正になる | 対話から。未完成のコードは「あるべき姿」の証拠ではないため |
| 計画 | 全機能 `built:`、**1回の適用掃引**が全機能の関所18を閉じる | 未完了の機能ごとに関所が走り、既存コードを仕様へ**突き合わせて直す**（ゼロから作り直さない） |
| その後 | 掃引通過 → merge → **タグ `1.0.0` — `Baseline: verified` なら、現状が検証されたベースラインとして固定される。** 新しい作業は `1.1.0` の差分として、`specs/1.1.0/request/` のメモから始まる | コードが仕様に届いたとき版が終わり、他の版と同じように検収される |

**混在が普通で、機能ごとに宣言できます。** 15機能は完成、3機能は道半ば — `Existing assets` に `Authority: as-built` と書き、3機能に `<!-- authority: to-spec -->` を付けます（[`spec-format.md`](../.claude/skills/hora/references/spec-format.md) の "`authority`"）。ステージ1の機能ごとの確認でその3機能に「まだ終わっていない」と答えれば、手で編集しなくてもちょうどこの形になります。

**`as-built` が買わないもの：** 関所18は必ず走ります — 適用掃引こそが、固定されたベースラインを「主張された」ものではなく「検証された」ものにします。未認証で到達できるものは、宣言が何であれ1操作ずつ尋ねられます。そして宣言が届くのは、宣言した時点で作られていた機能だけです — `1.1.0` で足す機能は、他の新機能と同じく対話で仕様化されます。

**もう1つ宣言があり、こちらは「どこまで検収してからタグを打つか」を決めます。** 同じ `Existing assets` 節の `Baseline:` 行で、値は2つです（[`spec-format.md`](../.claude/skills/hora/references/spec-format.md) の "Existing assets"）。

| | `verified` | `inventoried` |
|---|---|---|
| 各既存機能が負うもの | ユースケースと受入基準（他の機能と同じ） | **それか、名前と1行** — 機能ごとに `<!-- baseline: inventoried -->` で選ぶ |
| 適用掃引が見るもの | 全機能 | **listed でない機能だけ** |
| タグが主張すること | 検証されたベースライン | **検収したぶんだけ。残りは名前が残る** |
| verdict | `passed` | **`passed over 17 of 20 features; 3 not accepted`** — 素の `passed` は書けない |

**`inventoried` が買うもの:** 20機能ぶんの仕様と検収を待たずに次の作業を始められること。listed した機能は `_plan.md` の `## Not accepted` と全検収レコードの `not-accepted:` 行に残り続け、**次にその機能を触る版が仕様を書いて、フルライブで検収します。**

**買わないもの:** その機能が動くという確認。継承したテストが通ることだけが担保で、それ以上は何も主張されません。

**元が取れるかは、製品がどれだけテストされていないかに比例します。** 消える最大のコストは、テストの無い機能の受入基準を1件ずつ尋ねる往復です（テストがあれば草案されて、数機能ずつの確認で済みます）。**よくテストされた製品では宣言と機能ごとの選択にかかる往復が節約を上回るので、`verified` のままが正解です。**

---

## 始める前に

| | |
|---|---|
| **リポジトリが renchan / furo 系であること** | 関所は renchan と Furo の規約を述べたスキルに委譲します。別スタックのリポジトリでも順序と関所は効きますが、委譲される手順はすべて「それではないもの」を説明することになります |
| **バックエンドは1つ、DB システムも1つ** | 方針は `1 DB システム = 1 リポジトリ`。0個または2個以上は止まって尋ねます |
| **Node と npm**（キット自身の `npm install` 用） | これがキットを配置します。スキルはパッケージから来るもので、このテンプレートのツリーには入っていません |
| **Claude Code** | |

**フロントエンドは任意です。** スマホアプリ向け API だけ、という案件もあります。

---

## 目指す形

```
myproject-app/                  ← キット。specs/ .hora/ .claude/ を持つ。アプリコードは持たない
  legacy-api/                   ← 既存のバックエンド。手つかず
  admin-console/                ← 既存のフロントエンド。手つかず
```

**入れ子は git の要求ではなく Claude Code の要求です。** セッションは作業ディレクトリの外に書けないので、触る必要のあるリポジトリは中に入っている必要があります。

**ディレクトリ名は変えなくて構いません。** `Directory` 列はそのために在ります。

---

## 手順1 — 既存リポジトリの周りにキットを作る

新規プロジェクトと同じ手順で `<myproject>-app` をこのテンプレートから作り（[README.ja](../README.ja.md) の「始め方」）、既存リポジトリを中に移します。

```sh
mv legacy-api admin-console myproject-app/
cd myproject-app
npm install
```

**移動するか、新しく clone してください。シンボリックリンクは駄目です。** リンクされたリポジトリは、リポジトリ単位のコマンドがすべて依存している作業ディレクトリ規則を壊し、しかも壊れ方が間接的です — コマンドは走り、間違った設定を読み、もっともらしい結果を返します。

**既存の `.git` には何もしません。** [`hora-setup` の skill](../.claude/skills/hora-setup/SKILL.md) にある `rm -rf .git && git init` は**boilerplate を新規 clone した場合**の手順で、他人のコミット数百件が製品リポジトリの `main` に着地しないためのものです。**既に存在したリポジトリはこれを丸ごと飛ばします** — キットはその上に**被せる**のであって、**上書きする**のではありません。

---

## 手順2 — 既存の文書を持ち込む

**`/hora-spec` を実行する前にやってください。** セッションは自分の作業ディレクトリの中しか読めず、`/hora` が読むものはすべて `specs/<version>/spec.md` からのリンクを辿って到達されます。**wiki に置かれた要件定義書は、ステージ0 が開けない文書です。**

**そのために3つのディレクトリが空で同梱されています。** 文書ごとに、当てはまるものへ入れてください。

```
myproject-app/
  specs/
    1.0.0/
      spec.md               ← 入口。空で同梱され、/hora-spec が埋める
      sources/              ← それ自体が「仕様」である文書
        api-reference.md
        requirements.md
      annex/                ← 仕様を「説明する」文書
        screens.pdf
        er-diagram.png
        old-design-doc.md
      request/              ← 今それを作ってほしい、という要望。あなたの言葉のままで
        this-quarter.md
  legacy-api/               ← 手順1で移した既存リポジトリ
  admin-console/
```

**どこに入れるかという選択が、本来なら文書ごとに尋ねられていた質問そのものです。** ステージ0 はすべてを読み、あなたの置き方を確認として返し（*「`sources/` に入っているので、仕様の一部として扱います。合っていますか」*）、表を書きます。**20文書が、1回のやり取りと「これは移したい」という指摘だけになります。**

**`request/` だけは、既にある文書のためのものではありません。** ここに入れるのは「作ってほしいもの」です — メール、チケット、箇条書き1ページ、手元にある言葉のままで構いません。ステージ0 はそれを**この版の議題**として読み、7つのステージが節に起こして、1節ずつ承認を取ります。中身がそれ自体で仕様テキストになることは決してなく、`/hora-plan` はそもそも読みません。

**どのディレクトリも必須ではありません。** `specs/<version>/` 配下のどこに置いても同じように見つかり、その場合は確認ではなく質問になるだけです。**元の構成をそのまま持ち込みたければそれで構いません** — 構造上の規則は「`spec.md` が版ディレクトリ直下にあること」の1つだけで、それ以外の名前・入れ子・深さは自由です。

### 何を持ち込むか

| 持ち込むもの | 理由 |
|---|---|
| 要件一覧、API リファレンス、データ辞書 | 仕様そのものの一部になりうるため |
| モックアップ、画面デザイン、ER 図、フロー図 | 画面とモデルが何のためにあるかを説明するため |
| 古い設計書。陳腐化していても | 陳腐化していても証拠ではあり、ステージ0 が「古い」ことごと記録します |
| 誰かが実際に運用している表計算 | ある規則が唯一書き残された形である場合が多いため |

**バイナリで構いません。** PDF、PNG、書き出したモックアップ — リンクして内容を1行で説明するだけで、**「描かれた画面」が「述べられた要件」であるかのように書き写されることはありません。**

### 間違えやすい2点

**実装リポジトリの中へリンクしないでください。** `legacy-api/docs/` は gitignore されているので、そこへのリンクは**あなたのディスクでは解決し、他の全員のクローンでは切れます** — しかも静かに切れます。必要なものは `specs/<version>/` 側にコピーしてください。

**資料は1つの版の中に閉じ、版をまたいで共有しません**（[`structure.md`](../.claude/skills/hora/references/structure.md) の不変条件3）。1.1.0 で同じ文書が要るなら、そちらにコピーを置きます。冗長に見えますが意図的です — 共有すると、1.1.0 のために資料を直した瞬間に、1.0.0 が「当時とは違う資料」を指すことになります。

### このディレクトリが実際に決めていること

はじめの2つは `spec.md` の中の2つの表に対応します。そして**違いは「整理の仕方」ではなく、「その文書に書いてあることをキットが作るかどうか」です。**

| | 意味 | `/hora` の扱い |
|---|---|---|
| **`Sources`** | この文書は仕様**そのもの**の一部 | **feature ファイルとまったく同じ規則で読む。** 中身はタスクになり、**実装される** |
| **`Annex`** | この文書は仕様を**説明する**もの | 解釈のためだけ。**決してタスクを生まない** |
| **`request/`** | これは誰かが**欲しいもの**で、まだ誰も詰めていない | `/hora-spec` がそこから節を起こし、あなたが承認する。**表にも載らず、タスクにもならず、`/hora-plan` は読まない** |

**3行目があるから、粗いメモを安心して渡せます。** 同じ願望リストを `sources/` に入れれば願望がそのままタスクになりますが、`request/` に入れれば、一つ一つがまず質問か提案としてあなたに返ってきます。

**つまり `sources/` に入れた文書に5年前の「Slack 連携」の一行が残っていれば、それは誰かが実装する Slack 連携です。** `annex/` なら同じ一行は背景資料で、ステージ4があなたに何を確認するかに影響するだけ。**あなたが是としたものだけが仕様書に入ります。**

**だから分ける基準は品質ではなく、「誰かがその内容に責任を負えるか」です。** 現行の API リファレンスは `Sources`、2年前の設計書はどれだけ出来が良くても `Annex`。

**迷ったら `annex/` へ。** 失われるものはありません — 内容が本当に必要なら、対話を通って仕様書に入り、そこから機能になります。遠回りの分だけ増えるのは「人が一度読んで是と言う」という一段で、それはまさに `sources/` が飛ばしている一段です。

手で表を書き、どちらのディレクトリも使わない方法も使えます。書式は [`spec-format.md`](../.claude/skills/hora/references/spec-format.md) の "Directory layout" にあります。

### 持ち込めないものがあるとき

**存在することだけでも伝えてください。** ステージ0 は、自分が到達できない場所にあるもの — wiki、ドライブ、チケット管理 — を必ず尋ねます。**一番役に立つ文書が、誰も言い出さなかった1つだった、というのが常だから**です。名前を挙げてもらえれば、ファイル自体が届かなくても、その中身は対話を通じて仕様に届きます。

---

## 手順3 — 「既にあるもの」として仕様書を書く

**`/hora-spec` を実行してください。** ステージ0で既にあるものを読み、空の仕様書をコピーし、7つのステージ（[`stages.md`](../.claude/skills/hora-spec/references/stages.md)）を通して1節ずつあなたと書きます。手でコピーして埋める方法も使えます。同じ文書になります。

```sh
cp specs/skeleton/spec.md specs/1.0.0/spec.md
```

**製品を口述することは求められません。** 既存の20機能を記憶から、しかも厳密な書式で説明させれば、人は覚えているものだけを説明します。そして**語られなかった部分の沈黙は、「そこには何も無い」と全く同じに読めます。** そこでステージ0が、リポジトリと、あなたが指し示した文書を読み、そこに現れているものを草案に起こし、**書き上げるためではなく訂正するために**あなたへ返します（[`investigation.md`](../.claude/skills/hora-spec/references/investigation.md)）。

**「読むもの」と「尋ねるもの」は別の一覧で、その線引きが設計の中心です。**

| 読んで、合っているか確認するもの | 読んでも分からず、尋ねるもの |
|---|---|
| どの操作が在り、何を返すか | それが誰のための機能か |
| どのテーブルに何が入っているか | なぜその設計になったのか |
| どの画面がどの操作を呼ぶか | 使う人が何を成し遂げようとしていたか |
| **いま誰がその操作を呼べるか** | **本来は誰が呼べるべきか** |
| | **各機能がどこまで実装済みか** |

**4行目が、既存プロジェクトへの適用が元を取る場所です。** 「サインインしていれば誰でも呼べる」は auth filter から読み取れる事実ですが、それが誰かの決定だったのかは、たいてい全く別の話だと判明します。

**読んだものが、そのまま要件になることはありません。** 読み取りは確認として提示され（*「こう読み取りました。合っていますか」*）、あなたが是とするか訂正したものだけが書かれます。キット側の思いつきは提案として別に区別されるので、**提案が「システムが既にそうなっている」として文書に入ることはありません**（[`asking.md`](../.claude/skills/hora/references/asking.md)）。

**回答は、可能な限り選択肢として提示されます。** 保持期間の質問には現在の行数が、認可の質問には現在のフィルタが、`built:` の質問にはステージ0がその機能について見つけたものが添えられます。**書き起こすより、直すほうがはるかに多くなります。**

とくに効くステージが2つあります。

| ステージ | 適用時に効く理由 |
|---|---|
| **1（想定ユースケース）** | 誰も書き残していない挙動が製品にはあります。ここでそれが言語化され、「実際の動き」と「本来望まれていたもの」の差が見えるようになります |
| **6（セキュリティ）** | 呼び出し元が決められたことのない操作が、すでに動いています。現在のフィルタを読んで人の前に置けば、その全部が見つかります — 驚かれた1つ1つが「誰も決めていない認可」であり、それぞれが最初の検収掃引で確認される基準になります |

各節の説明は [`spec-format.md`](../.claude/skills/hora/references/spec-format.md) にあります。適用時にはそのうち3つがいつも以上に重要です。

### 3.1 リポジトリ構成表に `Directory` 列

```markdown
## 2. Repository layout

| Repository | Origin | Role | Directory |
|---|---|---|---|
| `myproject-backend` | renchan | the API and jobs (holds the DB) | `legacy-api` |
| `myproject-frontend-admin` | furo | the admin screens | `admin-console` |

### 2.1 Servers

| Server | protocol | consumer |
|---|---|---|
| `admin-graphql` | GraphQL | `frontend-admin` |
| `worker` | — | an API server in the same repository (no contract needed) |
```

| | |
|---|---|
| **`Directory` を書くと** | `/hora-setup` はそこを見て、**clone しません。** 書かれていること自体が「既に存在する」の宣言です。無ければ、その名前の上に何かを作る代わりに止まって尋ねます |
| **`Repository` 列も依然として重要** | `target` の値は**この列**から来ます。`Directory` からではありません。`target` は `.hora/tasks/` に残る恒久的な分類で、ディレクトリは誰かのディスク上の場所です。後でフォルダを改名しても `.hora/` の中身は動きません |
| **既定名に従う行では列を省く** | 両方が混在するのはごく普通です |

**サーバー表は省略できません。** contract はここから導出され、どのフロントエンドがどの contract を読むかもここで分かります。

### 3.2 既に存在する機能すべてに `built:`

適用を可能にしているのがこの注釈です。

```markdown
## Attendance
<!-- id: attendance -->
<!-- target: backend, frontend-admin -->
<!-- built: frontend -->
```

| 値 | 意味 | n/a になる関所 |
|---|---|---|
| *(省略)* | まだ何も無い | 無し — 新規機能の通常ケース |
| `spec` | 仕様はある。コードは無い | 1–2 |
| `backend` | バックエンドゲートぶんは既にある | 1–9 |
| `frontend` | フロントエンドゲートぶんも既にある | 1–17 |

**関所18 はどの値でも覆われません。** 何を書いても `[ ]` のまま残ります。それが設計のすべてです：**適用は動くものを作り直さないが、本当に動くのかは確かめる。**（`Baseline: inventoried` で listed にした機能は例外で、そちらは関所18も走りません — 代わりに**未検収として名前が残ります**）

**`/hora-spec` が導出し、機能ごとにあなたと確認します。** 半端な画面と完成した画面はファイル一覧からは区別できないので、キットは材料 — resolver、マイグレーション、テスト、画面 — を並べた上で、作文ではなく選択で答えられる形に用意します。その形は `Authority` 宣言で決まります：

- **`as-built` の下では**、導出した表をまず丸ごと提示し、そのあと機能ごとに、導出したゲートをデフォルトとして選択式で確認します。「まだ終わっていない」と答えた機能には `built:` の代わりに `<!-- authority: to-spec -->` が付きます。
- **`to-spec` の下では**、`built:` は聞かれも書かれもしません — 全関所が走り、コードを仕様へ突き合わせて直します。機能ごとの対話は同じように材料を添えて行われますが、決めるのは「仕様に何と書くべきか」 — ユースケースと、仕様とコードの食い違いの扱い — で、あなたに委ねられる部分が大きくなります。

```
#attendance — 導出: frontend（resolver 4本、マイグレーション1本、
テスト31本、それらを呼ぶ画面2つ）

  frontend（導出値） / backend / spec / まだ終わっていない（to-spec）
```

**手で書く方法も使えます** — 注釈はどちらでも同じものです。選べないのは「キットが推測すること」だけで、宣言の無い機能は、コードがどれだけ完成して見えても関所1から計画されます。

**`built:` を付けた機能にも、想定ユースケースと受入基準は書いてください。** 関所18 はそれに対して検証するので、どちらも無い `built:` 機能は「何に対して検収するのか」が存在しません。

**機能ごとの基準は、その機能自身の関所で確認できる範囲に留め、機能を跨ぐ流れは版そのものの受入基準に置いてください。** 既存のテストから草案する経路では特に注意が必要です — 4機能を跨ぐ統合テストは、どの機能から読み始めてもその機能の基準に見えるので、そのままその機能のブロックに書くと関所が絶対に満たせない前方参照になります。行き先は仕様書の `Version acceptance criteria` 節で、**`none` も正当な答え**です。跨ぐ流れが無い版はそう書いて先に進みます。

**例外は `listed` にした機能だけで、そちらは逆に書いてはいけません。** `Baseline: inventoried` の下で listed にした機能が持つのは、名前と1行、そしてデータモデルと操作一覧の行だけです。関所18 がそもそも走らないので検証する相手が要らず、**両ブロックを持つ listed 節は「仕様化と棚卸しを同時に主張している」ことになって `/hora` が止まります。**

### 3.3 既存資産

```markdown
## 5. Existing assets

Current implementation: legacy-api, admin-console (adopted in place)
Treatment: keep it — Hora Kit is being adopted onto these repositories, not used to rewrite them
Authority: as-built — what these repositories do is what 1.0.0 is
```

**`Treatment` は本来別のことを意味します** — 「この古いコードを新リポジトリに移植する」か「振る舞いだけ合わせて書き直す」か。適用の文脈では正直な答えはたいていそのどちらでもなく、**ここでそう明記しておくことが、ある関所が「書き直す」と判断するのを止めます。** `Authority` が何であっても必須のままです。

**`Authority` は[「最初に、2つの適用のどちらかを決める」](#最初に2つの適用のどちらかを決める)の宣言で**、コードのあるプロジェクトでは必須 — 無ければ `/hora` が止まります。例外の機能は自分の `<!-- authority: -->` を持ちます。

---

## 手順4 — `/hora` を走らせる

```
/hora
```

リポジトリが宣言されているのに全部は揃っていないと判定し、まず `/hora-setup` を走らせます。適用済みの行に対しては、こうなります。

| 手順 | 適用済み行での挙動 |
|---|---|
| 最新タグの取得、clone、`.git` の破棄 | **丸ごと飛ばす** |
| **除外リストへのディレクトリ登録** | **`.gitignore` と `eslint.config.js` の両方に追記される**（後述） |
| `package.json` の name/description | **プレースホルダのままの場合だけ**埋める |
| `.env.development` | **キーが空のままの箇所だけ**埋める |
| `docker.sh` / `docker-compose.development.yml` | **絶対に上書きしない。** 既にあれば読み、仕様書の手動検証表との差分を報告する |
| `npm install` | 実行する。その `postinstall` が両 hora パッケージからキットを配置する |
| `bank-id` の backend へのコピー | **まだ無い場合だけ** |
| 実物ツリーの読み取り | 実行し、読んだ時点の boilerplate タグとともに `.hora/tree/` に控える |

**そのどれもが独立した冪等チェックであって、全部か無しかのスキップではありません。** 人が既に埋めたものは上書きされません。

### 除外リストが見た目より重要な理由

`.gitignore` とルートの `eslint.config.js` は、実装リポジトリを**名前で**除外しています（`*-backend*/`, `*-frontend*/`）。`legacy-api/` はどちらにも当たらず、**どちらの失敗も無言です。**

| | 起きること | 気づく経路 |
|---|---|---|
| `.gitignore` | バックエンド全体が追跡され、**キットのリポジトリに**コミットされる | `git status` を読むまで無い。その時点で既にコミット済み |
| `eslint.config.js` | ルートの lint が、自分のものではない設定のリポジトリを走査する | そのリポジトリが同意していない規則に対する大量の違反 |

`/hora-setup` は宣言された `Directory` ごとに1行ずつ両ファイルに追記し、追記したことを報告します。**それが起きたか確認してください** — 適用手順の中で、抜けたときの損害が大きく、しかも静かなのはここだけです。

---

## 手順5 — 何かを作る前に計画を読む

次に `/hora-plan` が走ります。版を確定し、仕様書が決めていないことを尋ね、機能一覧を書きます。

**`built:` が正しいかを確認するのはこの瞬間です。** 計画には機能ごとに「いくつの関所が既に n/a か」が出ます。完成しているつもりだったのに何も宣言しなかった機能は関所1から計画され、`built: frontend` と宣言した機能は関所18 だけが開いた状態で並びます。

**どちらに間違えても、今なら安く、後だと高くつきます。**

| 間違え方 | どうなるか |
|---|---|
| `built:` と宣言したが実は作られていない | 検収が落とし、印が解除され、本当に作られる — **安全な方向** |
| 宣言しなかったが実は作られている | 動くコードに対して17の関所が走る。壊れはしないが、時間が無駄になる |

---

## 手順6 — 最初の検収掃引

開いている関所が18 だけの機能は、コードを書かず、ブランチも切りません。`/hora-build` はそのまま `/hora-accept` に入ります。

```
1. 環境の確認        ローカル E2E 群が実際に動くこと
2. ユニットスイート   既存のテストを、そのまま実行
3. シナリオ一覧      API が公開しているものから導出
4. 受入レビュー本体   到達可能性、CRUD の充足、アフォーダンス、
                     失敗と待ちを正直に伝えているか
5. UX の指摘         深刻度付き。プロジェクト context に対して
```

**手順1 で最初の実行が止まる可能性が高く、それは正常です。** 関所17 はローカルの E2E 環境を作るために在り、既存プロジェクトはたいてい**何かしら**（compose ファイル、シードスクリプト）を持っていますが、前提条件 — アプリの背後で全サービスが動き、各ロールでサインインでき、レビュー可能なデータがある — をまだ満たしていません。それを直してから再実行してください。

**飛ばさないでください。フロントエンド単体でレビューしないでください。** そのやり方で走らせたレビューは、**得ていない合格を報告します。**

### 指摘の意味

各指摘は「どの機能の、どの関所に戻すか」を書きます。適用済み機能の *built before Hora Kit was adopted* と印された区間に着地した場合、**その印は解除されます** — 直さなければならないコードは、結局のところ単に受け継いだものではなかったからです。影響する最も手前の関所から、本当に作ります。

これが、既存製品がキットの水準まで引き上げられていく仕組みです。**一度に1つの不足だけ、しかも不足が実際に示された箇所だけ。**

---

## 気をつけること

### boilerplate のタグが既存コードより新しい

`/hora-setup` は実物ツリーを読み、読んだ内容をタグとともに控えます。既存リポジトリは、現在の boilerplate が前提とする規約より前の世代かもしれません。**実物はあらゆる想定に勝ちます** — 読み取り手順が在る理由がそれです — が、委譲されるスキルは**現在の**規約を述べるので、同じリポジトリの中で新しいコードが古いコードと違って見えることがあります。それは想定内で、パッケージが既に離れた規約に対して新しいコードを書くよりは望ましい状態です。

### 履歴のあるリポジトリに `release/<version>` を切る

キットはどのリポジトリでも `release/<version>` の上で作業します。適用済み行では、そのブランチは fetch 後の `origin/main` から切られます（新規 `git init` からではありません）。空の `Release <version>` 開始マーカーは同じように付きます。

**`main` に直接コミットしないでください。** キットのリポジトリでは `main-guard.yml` が main への PR を `release/*` `hotfix/*` `dev` `env` に制限していますが、既存リポジトリにはその防壁が無いかもしれず、うっかり破りやすくなっています。

### 既存の eslint 設定と、こちらの設定

各リポジトリは自分の設定で、自分の中から lint します。キットのルート設定は実装リポジトリを**決して** lint しません — 除外リストはそのために在ります。既存設定が `@openreachtech/eslint-config` と食い違う場合、そのリポジトリの中では既存設定が勝ち、関所はそれに合わせて動きます。

### 既存のテスト

`/hora-accept` の手順2 で、そのまま実行されます。**通すために弱められることはありません** — スキップも削除も緩和も待ち回避もしません。既存テストが落ちたなら、それは迂回すべき障害ではなく**指摘**です。

### CI

`.github/workflows/` のワークフローは、リポジトリの公開状態に従います。private リポジトリなら `light` ラベルのセルフホストランナー、public なら GitHub の `ubuntu-latest` です。**どちらを使うかを手で書き換える箇所はありません。** private リポジトリで GitHub ホストに固定する場合は、4本のワークフローの `runs-on` の式を置き換え、`specs/<version>/spec.md` にその旨を記載してください — private リポジトリでは、GitHub ホストは実行ごとに課金されることを意味します。適用先リポジトリが元々持っているワークフローはそのままです。ここで言っているのは、キットが持ち込むワークフローだけです。

---

## 短くまとめると

```
1. このテンプレートから <myproject>-app を作り、既存リポジトリを中に移す
2. 既存の文書を specs/1.0.0/sources/（仕様そのもの — 要件一覧、API リファレンス）
   か specs/1.0.0/annex/（仕様を説明するもの — モックアップ、図、古い設計書）に、
   作ってほしいものを specs/1.0.0/request/（自分の言葉のままで。それ自体が
   仕様テキストになることはない）に入れる。3つとも空で同梱済み。どれも必須では
   ない。実装リポジトリの中へはリンクしない（gitignore 済みで、リンクは静かに切れる）
3. どちらが正かを決める：as-built（今動いているものが 1.0.0）か
   to-spec（仕様が正で、コードを追いつかせる）か。Existing assets に書き、
   これが質問の量を決める
4. specs/1.0.0/spec.md を書く（/hora-spec が既存を読んだ上で対話しながら書きます）：
     - ステージ0 がリポジトリと、置かれた文書を読み、置き方で示された
       sources / annex の区別を確認して表を書き、仕様とコードの食い違いを
       振り分けのために全件記録する
     - リポジトリ構成表に、既存リポジトリごとの Directory 列
     - as-built なら：導出された built: の表をまず丸ごと提示し、そのあと
       機能ごとに選択式で確認 —「まだ終わっていない」と答えた機能は
       authority: to-spec になる。ユースケースと受入基準は動いている
       システムから草案され、あなたは訂正するだけ
     - to-spec なら：built: は一切書かれない — 全関所が走り、コードを仕様へ
       突き合わせて直す。ユースケースは対話から、材料を添えて
     - 既存資産: そのまま活かす。加えて Authority の行
5. /hora
     - setup は clone を飛ばし、両方の除外リストにディレクトリを登録し、
       プレースホルダのままの箇所だけ埋める
     - plan が未決事項を尋ねる。全機能 as-built の版は1回の適用掃引として計画される
6. 計画を確認する：built: は全部正しいか？
7. 最初の検収掃引が、製品が実際に何をできるのかを教える
8. as-built なら：掃引通過 → merge → タグ 1.0.0。ベースラインが固定され、
   新しい作業は 1.1.0 で — specs/1.1.0/request/ にメモを1枚置けば足りる。
   to-spec なら：関所が機能ごとに、コードを仕様へ突き合わせて直していく
```

---

## 次に読むもの

| | |
|---|---|
| 各コマンドの詳細 | [`commands.ja.md`](./commands.ja.md) |
| なぜこの設計なのか | [`architecture.ja.md`](./architecture.ja.md) |
| 関所が委譲するスキル群 | [`skills.ja.md`](./skills.ja.md) |
| 仕様書の書式 | [`spec-format.md`](../.claude/skills/hora/references/spec-format.md) |
| ステージ0と、仕様書を書く7つのステージ | [`stages.md`](../.claude/skills/hora-spec/references/stages.md) |
| ステージ0が何を読み、読んでも決まらないものは何か | [`investigation.md`](../.claude/skills/hora-spec/references/investigation.md) |
| 「確認」と「提案」の違い | [`asking.md`](../.claude/skills/hora/references/asking.md) |
| 18の関所 | [`checkpoints.md`](../.claude/skills/hora-build/references/checkpoints.md) |
